properties:
  "mail.smtp.ssl.enable": true
redis:
  host: localhost
  database: 0
  port: 6379
  jedis:
    pool:
      max-active: 8
      max-wait: -1ms
      max-idle: 8
      min-idle: 0
    timeout: 30000ms
